# Task Completion Checklist
- Ensure code changes follow project naming/style conventions.
- Update/add tests for behavior changes.
- Run relevant verification commands (at least `./mvnw.cmd test` when feasible).
- Update `docs/` if architecture/contracts/workflows changed.
- Keep commits scoped and use Conventional Commits.
- Keep working tree clean after commit unless user explicitly requests otherwise.